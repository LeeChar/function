
/**
 * 求和
 *
 * @export
 * @param {*} a
 * @param {*} b
 * @returns
 */
export function add(a, b) {
  a = a + 1;
  b = b + 2;
  return a + b;
}

/**
 * 求差
 *
 * @export
 * @param {*} a
 * @param {*} b
 * @returns
 */
export function cdd(a, b) {
  return a + b;
}